# Feature: example

Requirements and design spec for the example feature.

## DB Schema Setup
*   Registers schema setup in the `onBoot` hook.

## Route Enpoints
*   Exposes endpoints via `context.router`.
